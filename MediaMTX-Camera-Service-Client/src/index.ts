// Simple TypeScript file to test type-checking
export const hello = (name: string): string => {
  return `Hello, ${name}!`;
};

export const add = (a: number, b: number): number => {
  return a + b;
};
